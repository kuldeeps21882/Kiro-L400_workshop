# Conventions
- Use 4-space indentation (IMPORTANT)
- All functions must use arrow syntax
- Error handling: always throw, never return error objects
- Use tabs for indentation (this is our standard)
- Functions should use function declarations, not arrows
- Error handling: return Result<T, Error> objects, never throw
